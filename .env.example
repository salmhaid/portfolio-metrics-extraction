# Copy to .env and fill in. .env is gitignored — never commit real keys.
OPENAI_API_KEY=
# Exact model ID from your OpenAI account (see `client.models.list()` or the dashboard)
OPENAI_MODEL=
# DEBUG | INFO | WARNING | ERROR (default: INFO)
LOG_LEVEL=INFO
